
Bot final corrigido.

Inclui:
- correções de sintaxe/indentação
- logs visuais gerais
- moderação de convite com ban automático
- respostas automáticas básicas
- reações por palavra-chave
- sistema de famílias V4 por slash (/familia ...)
- convite por DM para entrar na família
- logs verdes de família por imagem
- Mongo automático para backups das famílias (MONGODB_URI)

Variáveis de ambiente:
- DISCORD_TOKEN
- MONGODB_URI (opcional, mas recomendado)
- MONGO_DB_NAME (opcional, padrão: scanbot)

Depois de subir:
- use /familia setlog para definir o canal de logs das famílias.
